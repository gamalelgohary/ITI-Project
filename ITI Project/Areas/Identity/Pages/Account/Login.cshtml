@page
@model LoginModel
@{
    
    ViewData["HideFooter"] = true;
    ViewData["Title"] = "Log in";
}

<div class="d-flex justify-content-center align-items-center vh-100 ">
    <div class="card shadow-lg p-4 rounded-4" style="width: 400px; margin-top:80px">
        <div class="text-center mb-3">
            <i class="bi bi-box-arrow-in-right fs-1 text-primary"></i>
            <h3 class="fw-bold mt-2">Welcome Back</h3>
            <p class="text-muted">Log in to your account</p>
        </div>

        <form id="account" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-2" role="alert"></div>

            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" placeholder="name@example.com" />
                <label asp-for="Input.Email">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="form-floating mb-3">
                <input asp-for="Input.Password" class="form-control" type="password" placeholder="password" />
                <label asp-for="Input.Password">Password</label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-3">
                <div class="form-check">
                    <input class="form-check-input" asp-for="Input.RememberMe" />
                    <label class="form-check-label" asp-for="Input.RememberMe"></label>
                </div>

                <a asp-page="./ForgotPassword" style="text-decoration:none">Forgot your password?</a>
            </div>

            <!-- Login Button -->
            <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary mb-2">Log in</button>



            <!-- Register Button -->
            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl"
               class="w-100 btn btn-lg btn-outline-secondary mb-3">
                Register
            </a>

            <!-- Divider -->
            <div class="d-flex align-items-center mb-3">
                <hr class="flex-grow-1">
                <span class="px-2 text-muted">OR</span>
                <hr class="flex-grow-1">
            </div>

            <!-- Google Login -->
            @* <a asp-controller="Account" asp-action="GoogleLogin" *@
            <button type="button"
               class="w-100 btn btn-lg btn-light border d-flex align-items-center justify-content-center">
                <img src="https://developers.google.com/identity/images/g-logo.png"
                     alt="Google" width="20" height="20" class="me-2">
                Login with Google
            </button>
            @* </a> *@





        </form>

        <hr />

        @if ((Model.ExternalLogins?.Count ?? 0) > 0)
        {
            <div class="text-center">
                <p class="text-muted">Or continue with</p>
                @foreach (var provider in Model.ExternalLogins!)
                {
                    <button type="submit" class="btn btn-outline-dark w-100 mb-2" form="external-account"
                            name="provider" value="@provider.Name">
                        <i class="bi bi-google"></i> @provider.DisplayName
                    </button>
                }
            </div>
            <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post"></form>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
