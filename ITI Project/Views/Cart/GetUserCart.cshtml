@model ShoppingCart
@{
    ViewData["Title"] = "User Cart";
}

<div class="container my-5">
    @if (Model != null && Model.CartDetails != null && Model.CartDetails.Count > 0)
    {
        <h2 class="fw-bold mb-4 text-center text-primary">🛒 My Shopping Cart</h2>

        <div class="table-responsive shadow-sm rounded-3">
            <table class="table align-middle mb-0">
                <thead class="table-primary">
                    <tr>
                        <th>Product</th>
                        <th>Image</th>
                        <th>Category</th>
                        <th>Unit Price</th>
                        <th>Quantity</th>
                        <th>Total</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.CartDetails)
                    {
                        <tr>
                            <td class="fw-bold">@item.Product.ProductName</td>
                            <td>
                                <img src="/Images/@(string.IsNullOrEmpty(item.Product.Image) ? "No image.jpg" : item.Product.Image)"
                                     class="rounded shadow-sm" style="width:80px; height:100px;" />
                            </td>
                            <td>@item.Product.Categories.CategoryName</td>
                            <td><span class="fw-semibold">@item.Product.Price EGP </span></td>
                            <td>
                                <div class="d-flex align-items-center justify-content-center gap-2">
                                    <a class="btn btn-sm btn-outline-secondary"
                                       href="/Cart/RemoveItem?productId=@item.ProductId">-</a>
                                    <span class="fw-bold">@item.Quantity</span>
                                    @if (item.Quantity < item.Product.Stock.Quantity)
                                    {
                                        <a class="btn btn-sm btn-outline-primary"
                                           href="/Cart/AddItem?productId=@item.ProductId&&redirect=1">+</a>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Out of stock</span>
                                    }
                                </div>
                            </td>
                            <td class="fw-bold text-success">
                                @(item.Product.Price * item.Quantity) EGP
                            </td>
                            <td>
                                <a href="/Cart/RemoveItem?productId=@item.ProductId"
                                   class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i>
                                </a>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-4">
            <h4 class="fw-bold">
                Total:
                <span class="text-success">
                    @(Model.CartDetails.Select(i => i.Product.Price * i.Quantity).Sum()) EGP
                </span>
            </h4>
            <a class="btn btn-lg btn-success rounded-pill px-4" href="/Cart/DoCheckout">
                <i class="bi bi-credit-card"></i> Checkout
            </a>
        </div>
    }
    else
    {
        <div class="text-center my-5">
            <div class="card shadow-sm p-5 border-0 rounded-4" style="max-width: 500px; margin:auto;">
                <div class="card-body">
                    <i class="bi bi-cart-x text-muted" style="font-size: 4rem;"></i>
                    <h3 class="mt-3">Your Cart is Empty</h3>
                    <p class="text-muted">Looks like you haven’t added anything yet.</p>
                    <a href="/Home/Index" class="btn btn-primary mt-3 rounded-pill px-4">
                        <i class="bi bi-shop"></i> Continue Shopping
                    </a>
                </div>
            </div>
        </div>
    }

</div>
